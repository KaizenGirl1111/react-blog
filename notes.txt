1. installation->required->middleware
 /*backend route*/
2. signin->token->res.cookie(key,value,attributes passed in object form)
 /*frontend POST call*/
3. Sign in , credentials , include
4. cors , in detail , origin , credentials 

# const BACKEND_URL = import.meta.env.VITE_BACKEND_URL; in vite project
# const BACKEND_URL = process.env.REACT_APP_BACKEND_URL; in npx create app